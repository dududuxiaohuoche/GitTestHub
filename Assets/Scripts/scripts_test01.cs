using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scripts_test01 : MonoBehaviour
{
    // Start is called before the first frame update
    private string 字符串 = "Third";
    void Start()
    {
        字符串 ="乖乖，这是第三次修改了"
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
