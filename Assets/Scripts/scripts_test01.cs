using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scripts_test01 : MonoBehaviour
{
    // Start is called before the first frame update
    private string ×Ö·û´® = "First";
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
